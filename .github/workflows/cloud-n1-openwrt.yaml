name: Cloud N1 OpenWrt

# ==============================================================================
# 斐讯 N1 盒子 OpenWrt 云编译工作流
# 架构：Amlogic S905D / ARM64（armsr_armv8_generic）
# LEDE master 已用 armsr 替代 armvirt
# 场景：校园网主路由模式（默认），可切旁路由
# 源码：Lean's LEDE  打包：ophub/flippy-openwrt-actions
# ==============================================================================

on:
  push:
    branches:
      - main
    paths:
      - 'README.md'
      - 'script.sh'
      - 'config.sh'
      - '.github/workflows/cloud-n1-openwrt.yaml'
  workflow_dispatch:
  schedule:
    - cron: 0 8 * * 0

env:
  REPO_URL: https://github.com/coolsnowwolf/lede
  REPO_BRANCH: master
  SCRIPT_SH: script.sh
  CONFIG_SH: config.sh
  KERNEL_VERSION: 6.1.y
  KERNEL_REPO_URL: breakingbadboy/OpenWrt
  KERNEL_TAG: stable
  PACKAGE_SOC: s905d
  KERNEL_AUTO_LATEST: true
  UPLOAD_FIRMWARE: true
  TZ: Asia/Shanghai

jobs:
  build_openwrt:
    name: Build N1 OpenWrt firmware
    runs-on: ubuntu-22.04

    steps:

    - name: Free Disk Space
      uses: jlumbroso/free-disk-space@v1.3.1
      with:
        tool-cache: true
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        swap-storage: true

    - name: Checkout
      uses: actions/checkout@v6
      with:
        ref: main

    - name: Disk space check
      run: echo "Free space:" && df -h

    - name: Install build dependencies
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo -E apt-mark hold grub-efi-amd64-signed
        sudo -E apt-get -qq update --fix-missing
        sudo -E apt-get -qq full-upgrade
        sudo -E apt-get -qq install -y \
          ack antlr3 asciidoc autoconf automake autopoint binutils bison \
          build-essential bzip2 ccache cmake cpio curl device-tree-compiler \
          fastjar flex gawk gettext gcc-multilib g++-multilib git gperf \
          haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev \
          libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev \
          libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev \
          libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full \
          patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools \
          qemu-utils rsync scons squashfs-tools subversion swig texinfo \
          uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        sudo timedatectl set-timezone "$TZ"

    - name: Clone source code
      run: |
        git clone --depth 1 $REPO_URL -b $REPO_BRANCH openwrt
        cd openwrt
        useVersionInfo=$(git show -s --date=short --format="date: %cd<br/>commit: %s<br/>commit hash: %H")
        echo "useVersionInfo=$useVersionInfo" >> $GITHUB_ENV
        echo "DATE=$(date "+%Y-%m-%d %H:%M:%S")" >> $GITHUB_ENV
        echo "tag_name=$(date +%Y%m%d)" >> $GITHUB_ENV

    - name: Run script.sh (pre-feeds customization)
      run: |
        [ -e files ] && mv files openwrt/files
        chmod +x $SCRIPT_SH
        ./$SCRIPT_SH

    - name: Update and install feeds
      run: |
        cd openwrt
        echo "src-git helloworld https://github.com/fw876/helloworld.git" >> feeds.conf.default
        ./scripts/feeds update -a
        sed -i 's/TARGET_CFLAGS += -DHAVE_MAP_SYNC/TARGET_CFLAGS += -DHAVE_MAP_SYNC -D_LARGEFILE64_SOURCE/' \
          feeds/packages/utils/xfsprogs/Makefile 2>/dev/null || true
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Generate .config via config.sh
      run: |
        chmod +x $CONFIG_SH
        ./$CONFIG_SH

    # ==============================================================================
    # 架构验证 + defconfig + 下载
    # LEDE master 的 defconfig 在遇到递归依赖时会 fallback 到 x86
    # 这里做强制纠正：defconfig 后如果不是 armsr_armv8，清空重写再 defconfig
    # ==============================================================================
    - name: Expand config and download
      run: |
        cd openwrt
        echo "=== defconfig 前的 target 行 ==="
        grep -E "^CONFIG_TARGET_" .config || echo "(empty)"
        make defconfig
        echo "=== defconfig 后的 target 行 ==="
        grep -E "^CONFIG_TARGET_" .config
        if ! grep -q "^CONFIG_TARGET_armsr_armv8_DEVICE_generic=y" .config; then
          echo "检测到 target 偏离，强制纠正为 armsr_armv8_generic"
          > .config
          echo "CONFIG_TARGET_armsr=y" >> .config
          echo "CONFIG_TARGET_armsr_armv8=y" >> .config
          echo "CONFIG_TARGET_armsr_armv8_DEVICE_generic=y" >> .config
          echo "CONFIG_TARGET_ROOTFS_TARGZ=y" >> .config
          echo "# CONFIG_TARGET_x86 is not set" >> .config
          echo "# CONFIG_TARGET_MULTI_PROFILE is not set" >> .config
          make defconfig
          echo "=== 纠正后的 target 行 ==="
          grep -E "^CONFIG_TARGET_" .config
        fi
        if ! grep -q "^CONFIG_TARGET_armsr_armv8_DEVICE_generic=y" .config; then
          echo "FATAL: 架构纠正失败，仍然是 x86，请检查 script.sh 递归依赖"
          exit 1
        fi
        make download -j8
        find dl -size -1024c -exec rm -f {} \;

    - name: Compile firmware
      id: compile
      run: |
        cd openwrt
        echo -e "$(nproc) thread compile"
        make -j$(nproc) || make -j1 V=s
        echo "=== 编译完成 ==="
        df -h
        rm -rf ./build_dir
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Resolve kernel version
      if: steps.compile.outputs.status == 'success' && !cancelled()
      run: |
        if [[ ! "$KERNEL_VERSION" =~ \.y$ ]]; then
          echo "Using fixed kernel version: $KERNEL_VERSION"
          echo "RESOLVED_KERNEL_VERSION=$KERNEL_VERSION" >> $GITHUB_ENV
          exit 0
        fi
        kernel_series="$(echo "$KERNEL_VERSION" | awk -F '.' '{print $1"."$2}')"
        kernel_regex="${kernel_series//./\\.}"
        kernel_api="https://github.com/${KERNEL_REPO_URL}/releases/expanded_assets/${KERNEL_TAG}"
        latest_version="$(curl -fsSL "$kernel_api" | \
          grep -oP "(?<=[/>])${kernel_regex}\.[0-9]+(?=\.tar\.gz)" | \
          sort -urV | head -n 1)"
        if [[ -z "$latest_version" ]]; then
          echo "无法从 $kernel_api 解析内核版本 ${kernel_series}.y"
          exit 1
        fi
        echo "解析到最新内核版本: $latest_version"
        echo "RESOLVED_KERNEL_VERSION=$latest_version" >> $GITHUB_ENV

    - name: Package as N1 firmware
      if: steps.compile.outputs.status == 'success' && !cancelled()
      uses: ophub/flippy-openwrt-actions@main
      env:
        # armsr 产物路径：bin/targets/armsr/armv8/*.tar.gz
        OPENWRT_ARMVIRT: openwrt/bin/targets/armsr/armv8/*.tar.gz
        PACKAGE_SOC: s905d
        KERNEL_REPO_URL: ${{ env.KERNEL_REPO_URL }}
        KERNEL_VERSION_NAME: ${{ env.RESOLVED_KERNEL_VERSION }}
        KERNEL_AUTO_LATEST: false
        OPENWRT_IP: 192.168.2.10
        WHOAMI: shenlan

    - name: Organize output files
      id: organize
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        cd openwrt/bin/targets/armsr/armv8/ 2>/dev/null || {
          echo "armsr/armv8 目录不存在，检查产物路径"
          ls -la openwrt/bin/targets/
          exit 1
        }
        rm -rf packages \
               openwrt-armsr-armv8-generic.manifest \
               openwrt-armsr-armv8-Image \
               openwrt-armsr-armv8-generic-kernel.bin \
               sha256sums version.buildinfo feeds.buildinfo 2>/dev/null || true
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload firmware artifacts
      uses: actions/upload-artifact@v4
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: N1_OpenWrt_firmware
        path: |
            ${{ env.FIRMWARE }}/*.*
            ${{ env.PACKAGED_OUTPUTPATH }}/*.*

    - name: Create GitHub release
      uses: ncipollo/release-action@v1
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: ${{ env.DATE }} | N1-OpenWrt 校园网定制
        allowUpdates: true
        tag: ${{ env.tag_name }}
        commit: main
        token: ${{ secrets.actions_release }}
        body: |
          ## 编译信息

          **源码版本：**
          ${{ env.useVersionInfo }}

          **打包日期：** ${{ env.PACKAGED_OUTPUTDATE }}
          **内核版本：** ${{ env.RESOLVED_KERNEL_VERSION }}
          **架构：** armsr_armv8_generic（ARM64 / Amlogic S905D）
          **默认 IP：** 192.168.2.10
          **默认账号：** root
          **默认密码：** password
          **WiFi SSID：** N1-OpenWrt
          **WiFi 密码：** n1campus2026

          ## 刷机方式

          1. 下载 openwrt_s905d_n1_*.img.gz
          2. 解压得到 .img
          3. 用 balenaEtcher 写入 U 盘
          4. U 盘插入 N1 靠近 HDMI 口的 USB 口
          5. 通电开机
          6. 浏览器访问 http://192.168.2.10（或连 WiFi: N1-OpenWrt）
        artifacts: "${{ env.FIRMWARE }}/*.*,${{ env.PACKAGED_OUTPUTPATH }}/*.*"

    - name: Remove old releases
      uses: dev-drprasad/delete-older-releases@v0.3.4
      if: steps.organize.outputs.status == 'success'
      with:
        keep_latest: 3
        delete_tags: true
      env:
        GITHUB_TOKEN: ${{ secrets.actions_release }}
